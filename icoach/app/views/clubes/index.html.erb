<h1>Listing clubes</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Sigla</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clubes.each do |clube| %>
  <tr>
    <td><%= clube.nome %></td>
    <td><%= clube.sigla %></td>
    <td><%= link_to 'Show', clube %></td>
    <td><%= link_to 'Edit', edit_clube_path(clube) %></td>
    <td><%= link_to 'Destroy', clube, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Clube', new_clube_path %>
