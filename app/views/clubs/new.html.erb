    <h3 >New club</h3>

    <%= form_for @club, :url => clubs_path, :html => { :multipart => true } do |f| %>
    <% if @club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
        <% @club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :acronym %><br />
      <%= f.text_field :acronym %>
    </div>
    <%= f.file_field :avatar %>

    <div class="actions pull-right">
      <%= f.submit :class => "btn btn-success" %>
    </div>
<% end %>
<%= link_to "Back", :back %>